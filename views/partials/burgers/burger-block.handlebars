<li>
  {{burger_name}}

  <button class = "change-devour" data-id = "{{id}}" data-newdevour = "{{devour}}">
    {{#if devoured:false}}
      Devour it
      {{else}}
      Reorder
    {{/if}}
  </button>
</li>

